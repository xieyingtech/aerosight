## 1. 实施基础与数据库迁移

- [x] 1.1 建立可重复执行的增量数据库迁移目录和测试数据库初始化流程，保留 `db/schema.sql` 完整快照，并通过空库升级、现有快照升级与二次执行测试验证。
- [x] 1.2 增加 PostGIS 扩展检测与启用迁移，在缺少扩展时输出明确错误，并通过创建和查询 `geometry(PointZ, 4326)` 验证。
- [x] 1.3 新增项目级设备下行、实时总览、对象存储、外部算法和自动 AI 功能开关，并通过全部关闭时旧页面仍可用的回归测试验证。
- [x] 1.4 为 Web 和 worker 增加统一配置校验、请求/事件关联标识、结构化日志与敏感字段清理，并通过缺失配置和日志脱敏单元测试验证。
- [x] 1.5 建立 Web 集成测试与 Go worker 测试夹具，包括用户、团队、两个隔离项目、模拟无人机和模拟地面移动设备，并通过夹具自检验证。

## 2. 租户权限、审计与异步基础

- [x] 2.1 创建 `project_permissions` 和集中式权限解析服务，覆盖 `event:handle`、`mission:operate`、`mission:approve`、`algorithm:manage`、`agent:use`，并通过 owner/admin/member 权限矩阵测试验证。
- [x] 2.2 将项目资源查询和写入统一封装为从当前用户解析项目访问的服务端入口，并通过跨项目资源 ID 枚举负向测试验证无信息泄露。
- [x] 2.3 创建 `audit_events` 和审计写入 API，对受保护写操作实行审计失败即失败关闭，并通过故障注入测试验证无审计不产生副作用。
- [x] 2.4 创建 `idempotency_records` 及项目/主体/操作范围的幂等包装器，并通过并发重复请求测试验证只产生一次副作用。
- [x] 2.5 创建 `outbox_events`、领取/续租/完成/退避/死信逻辑和 worker 循环，并通过进程重启、重复投递和毒消息测试验证至少一次投递与幂等消费。
- [x] 2.6 增加项目事件单调游标与 `LISTEN/NOTIFY` 唤醒，在通知丢失时仍从 outbox 补处理，并通过故障测试验证游标连续或明确要求快照重载。

## 3. 统一设备接入与时空轨迹

- [x] 3.1 创建 `device_adapters`、`device_external_identities`、`device_connections`、`device_telemetry` 及复合项目外键，扩展设备类型和 capability，并通过跨项目绑定失败测试验证。
- [x] 3.2 定义版本化 adapter 上行 envelope、下行 command envelope、`Pose` / `Track` 契约和兼容策略，并通过无人机与模拟 ROS 设备契约测试验证相同轨迹 API、不同能力声明。
- [x] 3.3 实现管理员 adapter 配置、脱敏读取、连接测试与启停 API，通过角色授权和秘密不回显测试验证。
- [x] 3.4 实现设备发现、人工绑定、外部身份去重、能力更新和未实现 adapter 类型的显式拒绝，并通过冲突绑定与 `ros2` 未启用场景测试验证。
- [x] 3.5 创建 `coordinate_references`、`sensor_calibrations`、`observations`、`poses` 和轨迹索引，并通过时间、几何、质量字段及项目复合约束测试验证。
- [x] 3.6 实现遥测到观测/位姿的归一化摄取，保存采集/接收双时间、原始 CRS、WGS84、精度和来源链，并通过重复、乱序、时钟偏差和未知 CRS 测试验证。
- [ ] 3.7 实现心跳与 `online/degraded/offline/unknown` 状态投影，通过可控时钟测试验证转换原因和陈旧状态不会伪装成实时。
- [ ] 3.8 实现 simulator，可脚本化产生无人机与地面移动设备轨迹、电量、直播状态、媒体、ACK/NACK、超时和掉线，并通过端到端场景脚本验证。

## 4. 项目导航、地图总览与同步时间线

- [ ] 4.1 建立项目嵌套路由和固定侧边栏，包含总览、实时作业、任务、设备、告警事件、算法服务、智能体、数据资产和设置，并通过路由与权限可见性测试验证。
- [ ] 4.2 将 `/projects/:projectId` 设为项目总览默认入口，并通过直接 URL、项目切换和无权项目访问测试验证。
- [ ] 4.3 实现项目态势快照 API，返回设备最新位姿、活动任务、直播、媒体点、疑似违建、开放告警、区域和数据新鲜度，并通过同事务一致性及跨租户测试验证。
- [ ] 4.4 实现带游标、心跳、补发上限和权限复检的项目 SSE endpoint，并通过断线重连、游标补齐、超窗重载和成员撤权测试验证。
- [ ] 4.5 实现 MapLibre 图层模型与筛选，展示无人机、机巢、模拟 ROS 机器人、轨迹、航线/区域、媒体点、疑似违建和告警，并通过图层快照与项目隔离组件测试验证。
- [ ] 4.6 实现底部可折叠多轨时间线，按设备、任务步骤、媒体、算法运行、检测和告警分轨，并通过固定时空夹具验证顺序、聚合和缺失区段。
- [ ] 4.7 实现地图、时间线、右侧详情和媒体帧的双向选择与时间窗同步，并通过地图点选、拖动游标、范围选择和返回实时的 UI 测试验证。
- [ ] 4.8 实现实时/历史回放模式隔离，回放查询支持设备类型和时间空间过滤且禁止控制 API，并通过回放模式越权调用负向端到端测试验证。

## 5. 媒体资产与直播

- [ ] 5.1 抽象 S3 兼容对象存储接口和本地测试实现，支持预签名上传、HEAD/哈希校验和短时下载，并通过伪造键、跨项目键和校验失败测试验证。
- [ ] 5.2 扩展 `assets` 并创建 `asset_derivatives`、上传意图、`evidence_links` 和保全字段，实现对象写入 saga，并通过中断上传、重复完成和版本证据测试验证。
- [ ] 5.3 实现缩略图/预览派生 worker、失败重试和来源链，通过有效与损坏媒体夹具测试验证。
- [ ] 5.4 创建 `live_streams`，实现统一直播启动/停止、状态同步、短时播放 locator 和失败降级，通过 simulator 流、过期 locator 和无权访问测试验证。
- [ ] 5.5 实现总览和实时作业页的直播播放器、设备联动、延迟/断线状态和历史媒体切换，并通过无直播、直播失败与时间线选择测试验证。
- [ ] 5.6 实现媒体预览、播放、下载分级授权和敏感下载审计，并通过跨项目访问、过期签名和日志无永久凭据测试验证。

## 6. 无人机巡检任务与设备命令

- [ ] 6.1 创建 `task_versions`、`task_steps` 和任务草稿/发布模型，迁移现有任务为兼容版本，并通过旧任务可读及发布后不可变测试验证。
- [ ] 6.2 实现巡检区域/航线、步骤、无人机能力、触发、失败策略和媒体采集要求的 schema 校验，并通过合法模板与逐字段错误测试验证。
- [ ] 6.3 创建 `safety_policy_versions`，实现项目边界、禁限区、高度、速度、电量、时间窗和合规字段预检，并通过硬失败与警告场景测试验证。
- [ ] 6.4 实现按设备类型、能力、连接、电量、位置和占用状态的候选无人机排序，通过多候选、无候选和人工覆盖后重检测试验证。
- [ ] 6.5 创建 `approvals`，实现受保护动作、有效期和职责分离，并通过自批、重复批准、过期和合法独立批准测试验证。
- [ ] 6.6 扩展 `task_runs` 并创建 `task_run_steps`、`device_commands`、`command_attempts`，实现乐观状态机和命令账本，并通过合法/非法转换与未知 ACK 测试验证。
- [ ] 6.7 实现 worker 逐步调度、deadline、安全重试、暂停/恢复/取消/返航和紧急停止，通过 simulator 成功、NACK、断连、重启恢复与非幂等超时测试验证。
- [ ] 6.8 实现任务详情/实时作业 UI，展示预检、设备、步骤、命令确认和基于权限的操作，并通过只读成员前后端拒绝测试验证。

## 7. 可配置算法服务框架

- [ ] 7.1 创建 `algorithm_providers`、`algorithm_definitions`、`algorithm_definition_versions`、`algorithm_runs` 和 `algorithm_run_attempts`，并通过项目复合外键、不可变版本和幂等 run 约束测试验证。
- [ ] 7.2 实现算法服务管理 API/UI，配置 `type`、`baseUrl`、`secretRef`、认证、允许 header、超时、并发和速率限制，通过 owner/admin/member 授权与 key 不回显测试验证。
- [ ] 7.3 实现出站 URL 安全策略，校验 HTTPS、域名 allowlist、DNS 解析地址和每次重定向，拒绝 loopback、link-local、元数据地址与未授权私网，并通过 SSRF 套件验证。
- [ ] 7.4 定义算法 adapter 接口与 canonical result schema，覆盖输入资产/时空上下文、同步/轮询/callback、原始结果和版本化 mapping，并通过 JSON Schema 契约测试验证。
- [ ] 7.5 实现 MVP `http-json` adapter，使用预签名资产 URL、退避、熔断和 attempt 审计，并通过模拟同步、异步、超时、限流和格式漂移服务测试验证。
- [ ] 7.6 为 `kserve-v2`、`ogc-processes`、`ai-sdk` 注册明确 capability 与实现状态，已启用 adapter 通过协议契约测试，未启用 adapter 通过显式不可用测试验证不误报成功。
- [ ] 7.7 实现签名 callback 与 run 状态机，校验 provider、token、签名、重复回调和终态，并通过伪造、重放与乱序 callback 测试验证。
- [ ] 7.8 实现算法运行页和详情页，展示输入、状态、耗时、版本、mapping 诊断、原始结果引用与重试权限，并通过失败运行可诊断性 UI 测试验证。

## 8. 疑似违建识别与告警事件

- [ ] 8.1 创建 `suspected-construction` 算法定义模板及像素 bbox/polygon、标签、置信度和资产引用 mapping，并通过多种外部响应夹具测试验证 canonical detection。
- [ ] 8.2 实现巡检媒体完成后按项目配置触发算法 run，使用任务/资产幂等键，并通过重复媒体事件测试验证仅触发一次有效运行。
- [ ] 8.3 实现检测地理投影服务，使用位姿、相机标定和地理参考并保存方法/误差/版本；通过已知控制点及缺标定场景测试验证精度或图像级降级。
- [ ] 8.4 创建 `detections` 和 `detection_groups`，按空间重叠、时间窗和来源资产聚合疑似目标，并通过相邻帧、跨期目标和低质量位置夹具测试验证。
- [ ] 8.5 创建版本化 `event_rules`、`perception_events` 和 `event_feedback`，实现去重、严重度与处置状态机，并通过告警风暴、规则升级和并发处置测试验证。
- [ ] 8.6 实现告警列表/地图图层/详情证据链，统一使用“疑似违建”措辞并显示位置质量、模型版本、原图和标注，通过无地理位置与完整证据夹具测试验证。
- [ ] 8.7 实现确认、误报、类别修正、认领、调查、驳回和解决操作，以及可选 issue 关联，并通过权限、乐观并发和原算法结果不可变测试验证。

## 9. 时空大模型与安全调度

- [ ] 9.1 引入 Vercel AI SDK `ai` 和选定 provider packages，建立 provider registry 与部署配置校验，并通过至少一个模拟 provider 的流式调用测试验证可替换性。
- [ ] 9.2 定义服务端 `AgentExecutionContext` 和会话绑定，禁止从模型参数获取用户/团队/项目 scope，并通过伪造 `projectId` / `userId` tool args 测试验证。
- [ ] 9.3 建立白名单 tool registry、输入 schema、风险级别、所需 permission 和确认策略，通过 registry 快照测试验证不存在直连设备 adapter 的工具。
- [ ] 9.4 实现设备、任务、告警、资产、轨迹和地图上下文的只读工具，返回稳定引用、时间、质量和新鲜度，并通过跨项目注入与大结果截断测试验证。
- [ ] 9.5 实现巡检任务、报告和 issue 的草案工具，复用领域 schema 与证据引用，并通过提示注入测试验证不会绕过发布校验。
- [ ] 9.6 实现受保护的任务启动请求工具，使其经过 scope 鉴权、预检、审批、幂等、命令账本和 ACK，而非直连 adapter，并通过缺权限、缺审批与合法执行场景测试验证。
- [ ] 9.7 在排队 worker 真正执行 tool/调度前重新检查成员关系、项目 permission 和 context 有效期，并通过排队后撤权测试验证失败关闭。
- [ ] 9.8 实现智能体 UI、工具调用状态、证据引用和敏感结果最小留存，并通过临时 URL/秘密不进入消息历史及跨项目会话访问测试验证。

## 10. 告警自动化策略与报告

- [ ] 10.1 创建版本化 `alert_automation_policies` 和自动化运行记录，支持 `manual`、`agent-on-demand`、`agent-auto-draft`、`follow-up-draft`，并通过默认 `manual` 与历史版本测试验证。
- [ ] 10.2 实现告警事实事务与 AI outbox 解耦，确保模型失败不回滚或隐藏告警，并通过模型超时/不可用故障注入测试验证。
- [ ] 10.3 实现按需智能体总结和报告草稿，记录模型、提示模板、工具调用和证据版本，并通过证据修订后旧草稿过期提示测试验证。
- [ ] 10.4 实现自动报告/工单/后续任务草案，不自动发布或执行，并通过 `agent-auto-draft` 与 `follow-up-draft` 场景测试验证只产生草案。
- [ ] 10.5 实现项目级自动 AI kill switch 和管理员策略 UI，并通过运行中关闭策略测试验证后续动作停止且原告警仍可处置。
- [ ] 10.6 实现报告聚合、数据缺口、结论到证据导航、版本化发布与受权导出，并通过完整/不完整报告及证据保全测试验证。

## 11. DJI、治理与可观测性

- [ ] 11.1 在统一 adapter 后实现 DJI 测试适配器的签名验证、设备拓扑和 capability 映射，并通过脱敏回调夹具验证单元测试不依赖真实租户。
- [ ] 11.2 实现 DJI 遥测、直播控制和首批安全任务命令映射，对未授权或不支持能力明确降级，并通过厂商错误/NACK 夹具测试验证。
- [ ] 11.3 为无人机设备和运行增加实名登记、运行识别、批准、起飞确认、责任人和异常报告字段，并通过策略必需/豁免版本测试验证预检。
- [ ] 11.4 创建保留策略、`retention_holds` 和默认 dry-run 清理流程，并通过证据保全、普通到期资产、派生物和删除墓碑测试验证。
- [ ] 11.5 增加设备连接、摄取延迟、outbox、命令 ACK、直播、算法延迟/错误、告警、SSE、AI tool 拒绝和报告失败指标，并通过测试流量验证标签无秘密或高基数原文。
- [ ] 11.6 增加关键依赖健康/就绪状态及项目级降级原因，通过停用对象存储、算法服务、模型服务和 adapter 测试验证历史数据仍可访问。
- [ ] 11.7 建立审计查询和紧急停止演练入口，通过从用户/智能体请求到预检、审批、命令与 ACK/未知安全状态的完整关联链测试验证。

## 12. 纵向验收、性能与发布

- [ ] 12.1 编写 simulator 纵向验收：项目总览 → 无人机上线/轨迹 → 巡检预检/审批/执行 → 直播/采集 → 外部算法 → 疑似违建告警 → 人工处置 → 时间线回放，并通过自动化端到端测试验证。
- [ ] 12.2 编写智能体纵向验收：scope 内查询 → 报告/任务草案 → 受保护启动请求 → 设备 ACK，并验证跨项目、撤权后排队和提示注入全部失败关闭。
- [ ] 12.3 编写安全验收套件，覆盖 SSRF、秘密回显、callback 重放、跨项目枚举、重复命令、审批职责分离、回放控制、紧急停止失联和证据保全，并确保全部通过。
- [ ] 12.4 编写基准负载脚本，验证目标负载下遥测到地图/时间线 P95 ≤ 2 秒、canonical detection 到告警 P95 ≤ 5 秒且无重复副作用，并保存参数和结果。
- [ ] 12.5 编写部署、迁移、对象存储、秘密管理、算法 provider、AI provider、功能开关、kill switch、回滚和现场安全处置文档，并通过全新环境演练验证。
- [ ] 12.6 对现有数据快照执行升级与应用回滚演练，验证旧页面/数据继续可用、新证据不被删除、旧 worker 安全忽略未知事件。
- [ ] 12.7 运行 `pnpm check`、完整测试套件、`pnpm build` 和 `pnpm exec openspec validate build-air-ground-inspection-mvp --strict`，记录结果并修复全部阻断项后才允许试点开启。
