## 1. 现状基线与兼容迁移

- [ ] 1.1 盘点现有设备、Adapter、命令、直播和算法表/API 的可复用字段与冲突，形成实现注释并用现有 schema 测试确认没有平行账本
- [ ] 1.2 新增统一设备类别、关系、组件、外部身份约束和能力 schema 的增量迁移，并用迁移测试验证历史设备可回填且回滚不删除数据
- [ ] 1.3 补齐网络连接 profile、视频源/会话和算法定义版本所需的增量字段或表，并用数据库约束测试验证所有父子资源不能跨 `project_id`
- [ ] 1.4 更新数据库类型、fixture 和查询层，运行相关 TypeScript/Go 数据访问测试验证历史记录仍可读取

## 2. 统一设备领域层

- [ ] 2.1 实现开放的设备 kind registry、设备关系和组件领域模型，并用单元测试覆盖机场、飞行器、相机组件及未知设备类别
- [ ] 2.2 实现版本化 capability descriptor 与参数/状态约束校验，并用测试验证不支持或当前不可用的命令在 Adapter 前被拒绝
- [ ] 2.3 实现 Adapter 外部身份认领、重新绑定和冲突检测，并用集成测试验证内部设备 ID及历史引用保持稳定
- [ ] 2.4 实现统一在线/降级/离线/未知状态投影和数据新鲜度逻辑，并用超时、乱序和断线测试验证状态转换
- [ ] 2.5 将所有设备控制调用收敛到项目授权、安全策略和命令账本入口，并用跨项目和绕过调用测试验证不会发布下行消息

## 3. 通用算法服务平台

- [ ] 3.1 将算法 Provider 模型收敛为协议、端点、秘密引用、健康、超时和并发配置，并用 SSRF、脱敏及权限测试验证安全边界
- [ ] 3.2 实现不可变 Algorithm Definition Version、输入/参数 JSON Schema 和输出映射存储，并用发布/升级测试验证历史运行不会漂移
- [ ] 3.3 实现通用运行状态机与 canonical result union，覆盖分类、检测、分割、关键点、跟踪、OCR、表格/标量和自定义资产，并用 Adapter 契约测试验证成功、超时和重复回调
- [ ] 3.4 改造算法 API 返回动态能力目录和可渲染 schema，并用 API 测试验证不存在“违建”必填类型或固定类别
- [ ] 3.5 改造算法管理、运行和结果 UI 从目录/schema 渲染，将疑似违建迁为可删除 seed 模板，并用 UI 测试验证新增 OCR 定义无需修改前端源码即可显示和运行
- [ ] 3.6 隔离算法结果与设备控制，使用规则/任务草案连接后续动作，并用恶意 Provider 输出测试验证不能直接生成设备命令

## 4. 局域网与公网基础设施

- [ ] 4.1 为本地演示增加受认证的 MQTT 5 Broker 与 MediaMTX 服务配置，使用独立开发配置文件启动并验证 MQTT publish/subscribe 与测试流播放
- [ ] 4.2 实现 `lan`/`public` 网络 profile 校验器，覆盖 MQTT、HTTPS、WebSocket、媒体摄取和播放端点，并用回环地址、私网地址、无效 TLS 与匿名公网 Broker 测试验证策略
- [ ] 4.3 实现连接自检 API 与脱敏诊断结果，并用健康/失败 fixture 验证管理员能区分服务端已验证和待设备侧验证项目
- [ ] 4.4 补充局域网和公网示例配置、证书/域名、端口、防火墙、Topic ACL 与媒体权限文档，并按文档在干净环境完成一次配置校验

## 5. DJI MQTT 接入与产品矩阵

- [ ] 5.1 建立 DJI Adapter 配置和 Worker 租约生命周期，实现 MQTT 5 认证、重连与订阅恢复，并用临时 Broker 集成测试验证单活和断线恢复
- [ ] 5.2 实现 DJI Topic 路由与统一 envelope 摄取，覆盖拓扑、状态、事件、请求和服务回复，并用重复、乱序和身份不匹配测试验证幂等隔离
- [ ] 5.3 建立 Dock 2 + M3D/3TD 产品能力矩阵与协议 fixture，并用契约测试验证设备拓扑、状态、命令和视频源映射
- [ ] 5.4 建立 Dock 3 + M4D/4TD 产品能力矩阵与协议 fixture，并用契约测试验证新产品枚举不会误用 Dock 2 映射
- [ ] 5.5 实现未知型号/固件的只读降级和兼容性诊断，并用未知枚举 fixture 验证任何控制能力都不会误开放
- [ ] 5.6 将 DJI 发现与遥测写入统一设备/状态管线并发布实时更新，用 Worker + 数据库集成测试验证页面可查询已认领机场与飞行器

## 6. DJI 最小安全控制闭环

- [ ] 6.1 实现统一命令到 DJI services Topic/method 的版本化映射和业务 ID 关联，并用 ACK、NACK、错误 reply 和未知 reply 契约测试验证命令状态
- [ ] 6.2 实现航线任务下发、开始、取消和进度事件关联，并用协议 fixture 验证任务运行只根据已匹配回复推进
- [ ] 6.3 实现安全返航高优先级命令和超时未知状态处理，并用断线/迟到回复测试验证不会假定物理成功
- [ ] 6.4 实现能力驱动的机场调试命令白名单，包括支持项中的调试模式、舱盖、飞行器上下电、充电、声光告警和重启，并逐项用产品矩阵 fixture 验证
- [ ] 6.5 为高风险命令增加服务端权限、状态互锁、二次确认记录和审计，用跨项目、活动任务冲突和重复提交测试验证拒绝与幂等

## 7. 直播网关与播放器

- [ ] 7.1 实现统一视频源发现和稳定 source ID，将 DJI 飞行器/机场相机映射为设备 capability，并用 Dock 2/3 fixture 验证 UI 无固定槽位假设
- [ ] 7.2 实现直播会话状态机、随机摄取 key、租约和并发限制，并用数据库/领域测试覆盖启动、停止、重复停止、冲突和恢复
- [ ] 7.3 实现 DJI 启停直播命令与 MediaMTX 输入健康确认，使用 RTMP 摄取并用集成测试验证只有真实收到媒体才进入 `live`
- [ ] 7.4 实现项目授权的短期 HLS/WebRTC 播放信息与协议回退，并用未授权、过期令牌和首选协议失败测试验证保护与降级
- [ ] 7.5 实现 Worker 重启、设备掉线和会话过期后的直播协调清理，并用故障注入测试验证孤儿会话最终收敛

## 8. 能力驱动的设备与作业 UI

- [ ] 8.1 改造设备列表/详情为通用设备树，展示 kind、厂商、拓扑、状态新鲜度、组件与能力，并用 Dock 2/3 和非 DJI fixture 做组件测试
- [ ] 8.2 实现 DJI Adapter 配置向导及 LAN/Public 端点自检界面，并用权限与脱敏 UI 测试验证 member 无法查看或修改秘密配置
- [ ] 8.3 实现能力驱动的飞行器任务/返航和机场调试控制面板，并用不同产品矩阵测试验证按钮、参数、互锁和风险确认均由服务端能力决定
- [ ] 8.4 实现通用直播面板与播放器，可从任意设备视频源启动、停止和切换，并用机场/飞行器双源测试验证来源与会话状态正确
- [ ] 8.5 增加命令、连接和直播失败诊断展示，并用超时、NACK、离线、媒体未到流场景验证 UI 不显示虚假成功

## 9. DJI 协议级演示器

- [ ] 9.1 将现有 stdout simulator 扩展或替换为连接真实 Broker 的 DJI 协议客户端，并用测试验证它不直接写数据库或调用内部成功旁路
- [ ] 9.2 增加 Dock 2 + M3D/3TD 场景，发布拓扑、遥测和任务/机场命令回复，并通过统一设备与命令 API 完成自动化演示测试
- [ ] 9.3 增加 Dock 3 + M4D/4TD 场景及 NACK、超时、掉线和未知固件注入，并通过故障场景测试验证降级和恢复
- [ ] 9.4 为模拟机场和飞行器生成两路可辨识循环视频并走 RTMP → MediaMTX → 浏览器链路，用端到端测试验证来源切换和播放
- [ ] 9.5 提供一个不污染 `package.json` 业务脚本区的独立演示入口和操作说明，并在干净 checkout 按说明完成设备认领、控制、双直播和通用算法运行

## 10. 实机联调、安全与交付验收

- [ ] 10.1 编写 DJI 开发者账号、兼容固件、Pilot/Dock 配置和现场安全检查清单，并由连接向导输出与官方字段一致的配置摘要
- [ ] 10.2 在隔离项目以只读方式接入一套 Dock 2 + M3D/3TD，记录拓扑、状态和双类视频源证据后再验证受控命令
- [ ] 10.3 在隔离项目以只读方式接入一套 Dock 3 + M4D/4TD，记录拓扑、状态和双类视频源证据后再验证受控命令
- [ ] 10.4 分别完成局域网和公网端到端验收，验证设备上线、遥测、航线任务/返航、至少两项机场命令、无人机直播、机场直播和停止清理
- [ ] 10.5 运行租户隔离、Topic ACL、播放鉴权、秘密脱敏、重复命令和超时恢复测试，并保存无跨项目访问或重复副作用的结果
- [ ] 10.6 运行 `pnpm check`、`pnpm build`、Go 全量测试和 OpenSpec strict validation，修复所有失败并记录最终验证结果
- [ ] 10.7 按功能边界提交 conventional commits，确认最终 `git status` 仅保留用户原有无关修改且提交历史可独立审查
