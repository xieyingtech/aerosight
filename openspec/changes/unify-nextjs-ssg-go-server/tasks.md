## 1. 契约与兼容基线

- [x] 1.1 建立接口迁移清单，逐一列出全部 Route Handler 方法、Server Actions、服务端页面查询和新 API 对应关系；通过入口扫描确认没有未映射项。
- [ ] 1.2 固定旧接口成功/失败 fixture 和权限矩阵，覆盖 JSON null/空数组/ID、审计、幂等与凭据 envelope；运行原相关测试并记录基线。
- [x] 1.3 固定 Gin、中间件、sqlc 与 AI SDK 的兼容版本和选型来源；核对依赖安装、Go 版本及工具锁定可重现。

## 2. Go 应用与迁移骨架

- [x] 2.1 将 Go 模块整合到 apps/server 和 cmd/aerosight，保留维护命令并分离运行组件；运行存量 Go 测试，确认包迁移未改变后台行为。
- [ ] 2.2 建立 serve/migrate、配置校验、HTTP/worker 连接池预算和 dev 构建标签；用配置测试验证缺失密钥和无前端产物的开发启动边界。
- [x] 2.3 把迁移执行器移入 Go 并嵌入历史 SQL，保留 ledger、锁、checksum、基线和 PostGIS 检查；运行空库、旧库、并发、重复和 checksum 失败集成测试。
- [x] 2.4 添加 sessions 增量迁移和初始化流程，保持旧账号及凭据；验证首次启动、重复启动和旧库数据不被重置。

## 3. sqlc 数据层

- [x] 3.1 建立 sqlc 配置、查询目录、生成目录和固定工具入口，使用 database/sql + pgx stdlib；以至少一个读写查询验证生成代码可编译。
- [x] 3.2 建立 DTO 映射和 sqlc nullable/JSONB/ID 约定；通过契约测试验证类型化结果不改变 HTTP 序列化。
- [x] 3.3 建立共享事务入口，生成查询通过 WithTx 参与已有 *sql.Tx；用故障注入验证业务、审计、幂等、outbox 和项目事件整体回滚。
- [ ] 3.4 验证 PostGIS 显式投影与输入查询，并建立 schema 快照/迁移结果一致性检查；在真实 PostGIS 上通过坐标与 GeoJSON 往返测试。
- [x] 3.5 提供 pnpm db:generate/db:check，检查通过临时目录比较且生成代码提交；人为制造漂移验证失败，检查不覆盖工作文件。

## 4. HTTP 与认证基础

- [ ] 4.1 建立 Gin 路由分组和既有 http.Handler 接入，安装请求 ID、slog、恢复和错误映射；通过普通/流式异常与日志脱敏测试。
- [ ] 4.2 安装安全头、分组限流和可信代理处理，隔离急停与普通写额度；验证伪造转发头、429/Retry-After 和额度耗尽后的授权急停。
- [ ] 4.3 配置 context deadline、服务器读头/空闲期限、体积限制及静态 gzip；验证超时取消、SSE 不被截断和 Range 不被压缩改变。
- [x] 4.4 接入 SCS/postgresstore、bcrypt 和四个 auth API；测试历史密码、会话轮换、重启持久性、退出、过期和旧 Cookie 拒绝。
- [ ] 4.5 接入 gorilla/csrf，明确公开来源、开发 Cookie、独立回调组；测试登录/退出/写请求缺 Token、非法 Origin、有效代理请求和有效机器回调。
- [ ] 4.6 迁移用户/团队/项目权限服务及事务内复查；运行跨租户、显式权限别名、权限撤销和排队任务再授权测试。

## 5. 业务 API 迁移

- [ ] 5.1 使用 sqlc 迁移团队、项目、个人资料与平台管理读写，替换相关 Server Actions；依据清单逐项验证创建和权限拒绝行为。
- [x] 5.2 迁移项目概览、设备列表/树、地图态势、快照和回放数据 API；验证 PostGIS 数据、筛选和租户不可见契约。
- [x] 5.3 迁移设备适配器配置、发现绑定、连接测试、能力命令与 DJI 设置 API；复用 Go 驱动并验证加密兼容、审计和幂等。
- [x] 5.4 迁移 FlightHub 连接器创建、查询、测试、token 更新、同步和断开 API；运行现有 connector 与凭据测试。
- [ ] 5.5 迁移任务定义/运行、控制、急停演练、审计轨迹及报告 API；验证状态机、审批、原子性、重复命令和导出权限。
- [ ] 5.6 迁移事件处理、案件详情/协作和草稿 API；验证版本冲突、关联资源、权限别名和审计契约。
- [ ] 5.7 迁移算法 provider/definition/run/retry API；使用 sqlc 并验证配置校验、执行触发、版本与重试行为。
- [ ] 5.8 迁移直播启动/停止/播放、资产访问和 media-auth；验证原凭据、租户边界、Range、过期签名与 MediaMTX 请求契约。
- [ ] 5.9 迁移 AI provider 管理/测试、会话/消息与六个只读工具到官方 Go SDK；验证既有协议、20 条历史、8 步上限、证据引用、越权拒绝和上游故障。
- [x] 5.10 实现项目与频道 SSE，保留游标、回放上限、心跳与授权复查，按需借用数据库连接；通过续传、snapshot-required、登出撤权断流与连接释放测试。
- [ ] 5.11 将算法回调与算法资产 handler 接入统一端口；通过有效签名、过期/重放、超大请求及租户不匹配测试。

## 6. 前端静态化与开发代理

- [ ] 6.1 建立相对地址 API 客户端、CSRF 提交和登录失效处理，改造登录/退出；在 Next 开发入口和 Go 生产入口分别验证会话生命周期。
- [ ] 6.2 把公共布局、团队/项目列表、管理与个人页面改为静态壳和客户端加载；验证加载、空结果、失败与访问拒绝状态。
- [ ] 6.3 把项目各工作台与嵌套详情改为固定路径/查询参数并补 Suspense；在构建后新建资源，验证详情访问无需重建。
- [ ] 6.4 统一生成页面链接、报告与 AI 引用地址，实现 Go 307 与开发 Next redirects；测试全部旧路由映射、筛选保留、冲突参数、非法 ID 和前进后退。
- [ ] 6.5 在 Next 开发 phase 配置 beforeFiles API/资产 rewrites，生产只启用 export；测试 Cookie/Origin、SSE 首条消息与长连接取消、文件 Range，确认 Go 无开发代理。
- [ ] 6.6 移除迁完的 Route Handler、Server Actions、Auth.js、pg 与 TS 服务端 AI 依赖；通过扫描、typecheck 及无数据库/无密钥静态构建检查无运行时依赖回流。

## 7. 生产托管与统一生命周期

- [ ] 7.1 实现 out/migrations 校验复制与 embed 构建，提供静态 HTML、资源、HEAD、缓存和 404；验证缺产物失败、MIME、路径穿越与 API 不回退首页。
- [ ] 7.2 从静态 HTML 生成 CSP 脚本哈希并配置地图/媒体资源来源；在生产页面验证 hydration、地图 worker、直播及未允许脚本被阻止。
- [ ] 7.3 统一 HTTP、MQTT、outbox、调度和会话清理的启动/退出，更新健康和 readiness；验证必要组件失败、正常停止及租约恢复。
- [x] 7.4 采用 Prometheus 官方客户端接管现有指标并限制 /metrics 访问；验证业务指标名称/标签兼容、HTTP 指标和未认证拒绝。
- [ ] 7.5 更新 pnpm build/start/dev/db:migrate、单应用多阶段镜像和部署地址配置；在无 Node.js 运行镜像直接启动二进制，确认只有一个应用端口。

## 8. 整体回归与交付

- [ ] 8.1 按接口清单完成全部兼容验收，执行 pnpm check、pnpm build、pnpm db:check 与相关迁移/安全测试；记录结果且无未验证迁移入口。
- [ ] 8.2 完成单二进制端到端演练：登录、新项目、设备模拟接入、任务/算法回调、SSE、资产和 AI 假上游；验证生产包不依赖源码和 Node。
- [ ] 8.3 完成开发代理与生产直连对照测试及退出/重启负载演练；验证长连接释放、数据库连接预算与任务不丢失/不重复副作用。
- [ ] 8.4 执行旧库升级和应用回滚演练，更新 README、环境变量与 openspec/config.yaml 职责；交付备份、切换、重登录和回滚说明，不修改其他活动变更业务要求。
- [ ] 8.5 核对六个 capability 的全部场景与任务证据，归档前同步 delta specs 并运行 OpenSpec strict 校验及项目完整检查；记录最终交付状态。
