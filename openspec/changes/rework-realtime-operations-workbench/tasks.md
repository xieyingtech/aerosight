## 1. 服务端工作台投影

- [ ] 1.1 抽取设备树现有的“能力 → 授权动作”服务端派生逻辑供态势快照复用，并通过单元测试验证不同角色不会得到越权动作
- [ ] 1.2 扩展项目态势快照的设备投影，返回安全的类型、驱动显示信息、有效能力、动作可用状态和视频频道摘要，并通过 API 测试验证租户隔离及响应不含驱动密钥/连接凭据
- [ ] 1.3 为实时工作台定义共享 TypeScript 模型和快照刷新接口，并通过类型检查验证页面组件不再自行推断 DJI 型号能力

## 2. 只读项目总览

- [ ] 2.1 从现有地图能力抽取只读 `OverviewMap`，支持设备高亮和进入携带 `deviceId` 的实时作业链接，并通过组件测试验证地图本身不触发设备命令
- [ ] 2.2 在项目总览中用摘要与 `OverviewMap` 替换完整 `SituationExplorer`，并通过页面测试验证控制按钮、直播播放器和完整历史时间线均未渲染

## 3. 单设备实时作业上下文

- [ ] 3.1 实现 `RealtimeOperationsWorkbench` 的统一选择状态，解析并安全校验 `deviceId`/`streamId`，通过测试覆盖有效深链接、跨项目标识、越权标识和浏览器前进后退
- [ ] 3.2 实现可搜索设备选择器并接入所有可见设备，通过组件测试验证无坐标设备仍可选择和操作且地图展示明确的无位置提示
- [ ] 3.3 将地图点击、设备选择器、设备详情抽屉和 URL 双向同步，通过交互测试验证切换不会形成更新循环或保留错误设备上下文
- [ ] 3.4 将现有历史时间线迁为选中设备的默认收起次级面板，并通过测试验证总览不再加载该面板且实时作业按设备过滤

## 4. 能力驱动操作面板

- [ ] 4.1 在实时工作台中按服务端授权动作投影渲染通用设备操作面板，并通过组件测试覆盖可执行、无权限、离线和安全联锁禁用状态
- [ ] 4.2 将设备命令接入现有统一命令接口并在提交后刷新工作台快照，通过集成测试验证 RBAC 拒绝、冲突和设备失败均被准确展示且不会前端直连厂商 API
- [ ] 4.3 按选中设备能力渲染实时遥测、传感数据和诊断分区，并通过组件测试验证不支持的能力不会出现写死面板

## 5. 实时作业直播体验

- [ ] 5.1 实现项目级 `ActiveStreamSwitcher`，展示设备、频道和真实状态，并通过交互测试验证切换直播会同步设备上下文和 URL
- [ ] 5.2 重构直播播放器，仅在媒体确认可播放且取得签名播放信息时挂载，通过测试验证 requested、starting、stopping、failed 和无直播空状态不会误播
- [ ] 5.3 为设备视频能力实现频道选择与启动操作，向现有接口显式提交 `streamKey`，并通过 API/组件测试验证多频道选择、成功后原地选中新会话及并发限制错误
- [ ] 5.4 将停止直播迁入实时工作台并接入现有停止流程，通过测试验证 stopping 到 stopped/failed 的状态展示和活跃频道列表更新
- [ ] 5.5 实现命令后的立即刷新和直播过渡态有界轮询，通过假时钟测试验证稳定状态不会永久高频请求且超时后给出可恢复提示

## 6. 设备管理职责收敛

- [ ] 6.1 从设备树/详情移除日常控制和直播启停入口，保留资产、DeviceType、Driver、能力、连接与诊断信息，并通过页面测试验证职责边界
- [ ] 6.2 为设备管理增加携带 `deviceId` 的“进入实时作业”入口，并通过导航测试验证目标工作台自动选择对应设备

## 7. 集成验证与交付

- [ ] 7.1 使用本地模拟设备/直播会话完成浏览器演示测试，验证总览只读、地图与无坐标设备选择、能力操作、频道启动/切换/停止和错误态，并记录可复现步骤
- [ ] 7.2 运行 `pnpm check` 和 `pnpm build`，修复本次变更引入的问题并确认完整检查通过
- [ ] 7.3 按逻辑阶段创建 Conventional Commits，并通过 `git log` 和干净工作区确认实现提交可独立审阅
