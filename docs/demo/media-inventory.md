# Demo 历史航拍素材

2026-09-14：用户从司空网页手动下载到 Downloads；本地完成校验与去重复制。来源为司空媒体目录 321463914，文件拍摄日期为 2026-08-18。此次未通过 OpenAPI 下载，也未触发真实飞行。

## 本地盘点

| 类型 | 去重后数量 |
| --- | ---: |
| RGB 图片 | 6 |
| RGB 视频 | 11 |
| 热红外图片 | 5 |
| 热红外视频 | 11 |
| 合计 | 33 |

Downloads 中检查了 35 个文件，其中 2 个为 SHA256 相同的重复图片（0006_V、0012_T）。原始下载文件全部保留，项目素材副本按内容去重。

图片已用 Pillow 完整解码；视频通过 ffprobe 读取信息并用 ffmpeg 完整解码首个视频流。35 个文件均通过检查，复制后的文件通过 SHA256 一致性检查。此检查证明文件可读取，不代表拍摄内容满足检测效果或所有附属视频流均已验证。

本地目录（均在 Git 忽略的 `.build` 下）：

- 素材：`.build/msup-demo/source-media/{rgb,thermal}/{images,videos}/`
- 逐文件来源、大小、SHA256、尺寸、视频时长、重复映射：`.build/msup-demo/source-media/manifest.json`
- RGB 六图预览：`.build/msup-demo/source-media/rgb-contact-sheet.jpg`
- 校验脚本：`.build/msup-demo/prepare_media.py`

与此前网页 37 条记录相比，本地数量尚不一致；按序号配对，本地未见 0003_T 视频、0017_V 视频、0004_T 图片。这里仅列疑似缺项，未重新核对网页，不声称全量归档完成。当前素材足以开始闭环开发，无需等待补齐。

## 演示选材

六张 RGB 图片均为公园及周边航拍。优先检查 0009_V、0012_V 的道路车辆，以及 0016_V 的游乐场和步道；它们是候选样本，尚未运行 YOLO，不能提前承诺检测数量或效果。航拍目标较小，应先实测原图推理，再决定是否切片。

闭环口径采用“巡查目标识别 → 智能体结合证据提出待复核事项 → 人工复核 → Issue”，不把普通车辆或人员检测直接描述为违规认定。热红外素材保留用于平台多源资产展示，不用普通 RGB 预训练 YOLO 冒充热红外识别。

素材准备与独立 YOLO API 推理已完成；平台导入、同一次 Task Run 全链验证和人工标注效果评测仍待执行。

## 首次本地 API 验证

YOLO11n COCO，Apple MPS，imgsz=1280，confidence=0.25。六张 RGB 原图经 `/infer` 完成真实推理：0006_V 返回 8 个 car 框，0009_V 返回 2 个，0012_V 返回 8 个，其余三张为 0。跨图可能包含重复车辆，18 个预测框不代表 18 辆独立车辆，也不是人工确认的准确率。预览中仍有未框出的车辆，尚需标注评测或切片优化。上文“尚未运行 YOLO”为选材阶段状态，以本节实测为准。

此次六图推理计时约 43–270 ms/张（服务端计时，不含图片读取解码和完整 HTTP 往返，不是性能基准）。JSON、标框预览及测试汇总保存在 `.build/msup-demo/yolo-results/`。六图接口、坐标边界、真实 multipart 上传、损坏图片拒绝、校验和错误、目录穿越及未授权 URL 拒绝测试均通过。

启动及协议见 `infra/demo/yolo/README.md`；本次独立服务测试未创建平台 Run。
